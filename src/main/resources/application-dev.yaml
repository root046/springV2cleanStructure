server:
  port: '8081'
  servlet:
    context-path: /hr
spring:
  main:
    allow-circular-references: 'true'
  datasource:
    username: sa
    password: dev
    url: jdbc:h2:file:./database/db_dev
    driverClassName: org.h2.Driver
  h2:
   console:
    enabled: 'true'
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

users:
  get-user-info:
    url: http://localhost:8081/hr/mock-data/user?id=